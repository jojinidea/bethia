// leaf nodes are nodes with both NULL pointers as left and right children 

// if node == NULL, return 0
// otherwise, if left and right child nodes are NULL, return 1
// leaf count of a tree = leaf count of LST + leaf count of RST


